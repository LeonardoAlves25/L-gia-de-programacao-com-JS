function mensagem(){

    var base = parseInt(prompt("digite a abse do retangulo :"));
    var altura = parseInt(prompt("digite a altura do retangulo :"));
    var area = parseInt()

    area = base*altura; 
    alert("a area total do retangulo é :"+ area); 
}