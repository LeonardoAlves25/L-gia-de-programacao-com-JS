function mensagem(){
    var altura = parseInt(prompt("digite a altura do triangulo :"));
    var base = parseInt(prompt("digite a base do seu triangulo :"));
    var area = parseInt();

    area=(altura*base)/2
    alert("a area total do triangulo é :"+area)
}