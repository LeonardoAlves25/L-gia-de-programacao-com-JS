// 4 maneiras de criar variaveis
// 1 - var (ele te permite criar mais de uma variável com o mesmo atributo e/ou valor)
// 2 - let (não permite criar variávei com o mesmo atributo)
// 3 - const (constante/ variável cujo valor nunca se altera)
// 4 - (não usar variável, apenas concatenar valor)

let contadora=0;
const nome="anderson";


do{
    document.write(contadora+"</br>");
    contadora++;
} while (contadora<=10); 

//treino let

let contadora1 = "6"
alert(contadora1);

if (contadora1>"2") {
    document.write(contadora1);
} else{
    alert(contadora1);
}

// treino const

const nom= 16
 Document.write(contadora)